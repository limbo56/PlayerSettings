package me.limbo56.playersettings.command.subcommand;

import me.limbo56.playersettings.PlayerSettings;

public class DefaultSubCommand extends OpenSubCommand {
  public DefaultSubCommand(PlayerSettings plugin) {
    super(plugin);
  }
}
